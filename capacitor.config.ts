import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Countries-18002407',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
